# Al Adaam Scheduler - Environment Variables
# Rename this file to .env and fill in your values
# ⚠️ NEVER commit your actual .env file to version control!

# ===========================================
# APPLICATION SETTINGS
# ===========================================
VITE_APP_URL=http://localhost:5173
VITE_API_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173

# JWT Secret (for backend authentication)
JWT_SECRET=your-secure-random-string-here

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================
# For sending meeting confirmations and reminders.
# If RESEND_API_KEY is set, the email service uses Resend; otherwise emails are logged to console.
RESEND_API_KEY=

# Legacy SMTP fallback (unused when RESEND_API_KEY is set)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password
EMAIL_FROM=noreply@aladaam.com

# ===========================================
# CALENDAR SYNC — GOOGLE CALENDAR (Optional)
# ===========================================
# Create OAuth credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI must match GOOGLE_REDIRECT_URI.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/api/calendar/google/callback

# ===========================================
# CALENDAR SYNC — MICROSOFT OUTLOOK (Optional)
# ===========================================
# Register an app at https://portal.azure.com → Azure Active Directory → App registrations
# Add a redirect URI that matches MICROSOFT_REDIRECT_URI.
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_REDIRECT_URI=http://localhost:3001/api/calendar/microsoft/callback

# ===========================================
# ZOOM — NATIVE CONFERENCING (Optional)
# ===========================================
# Server-to-Server OAuth app — see https://marketplace.zoom.us
# Required for native Zoom meeting creation on bookings.
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=

# ===========================================
# HUBSPOT CRM (Optional)
# ===========================================
# OAuth app — see https://developers.hubspot.com/docs/api/oauth-quickstart-guide
# HUBSPOT_REDIRECT_URI must match the callback registered in the HubSpot app.
HUBSPOT_CLIENT_ID=
HUBSPOT_CLIENT_SECRET=
HUBSPOT_REDIRECT_URI=http://localhost:3001/api/crm/hubspot/callback
# Optional: when set, inbound HubSpot webhooks are verified via X-HubSpot-Signature-v3.
HUBSPOT_WEBHOOK_SECRET=

# ===========================================
# DEVELOPMENT
# ===========================================
NODE_ENV=development
